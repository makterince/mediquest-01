import React from 'react';

const MobileNavBackground = ({ closeMobileNav }) => {
  return <div className="mobile-nav-background" onClick={closeMobileNav}></div>;
};

export default MobileNavBackground;
